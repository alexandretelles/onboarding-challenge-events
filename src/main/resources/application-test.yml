spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/onboarding
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: 'true'
  liquibase:
    enabled: 'true'